import styled from "styled-components";

const AboutMe = styled.div`
  .intro {
    display: flex;
    img {
      height: 100px;
      width: 100px;
    }
    p {
    }
  }
`;

export default AboutMe;
